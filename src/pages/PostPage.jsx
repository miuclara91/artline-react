import React from 'react';
import Post from '../components/Post';

const Postpage = (props) => {

    return (
        <div>
            <Post userId={1}></Post>
        </div>
    );
}

export default Postpage;
